This is a generic mod that can be used as a base to develop additional mods.
It implements multiblock using meta and has a basic GUI, container and
networking configuration that currently does nothing.
